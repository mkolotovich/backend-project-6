extends ../layouts/application.pug

block header
  h1= t('views.tasks.new.header')

block content
  +formFor()(action='/tasks' method='post')
    +input(task, 'name', errors, 'Наименование')(type="text")
    +textarea(task, 'description', errors, 'Описание')
    +select(signInForm, taskStatuses, 'statusId', errors, 'Статус')
    +select(signInForm, users, 'executorId', errors, 'Исполнитель')
    +select(signInForm, labels, 'labels', errors, 'Метки', true)
    +submit(user)(value=t('views.statuses.new.submit'))
